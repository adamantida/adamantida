name: Generate Snake

on:
  schedule: # Run at 00:00 UTC every day
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3 # Обновлено до версии 3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16' # Можно указать '20', если ваша среда готова к работе с этой версией

      - name: Generate Snake
        uses: Platane/snk@v2
        with:
          github_user_name: adamantida
          outputs: dist/snake.svg

      - name: Push Snake
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: dist
